// Write an identity function that takes an argument and return the argument.
// identity(3) -> 3
function identity(input) {
  return input;
}

function print(input) {
  console.log(input);
}

print(identity(3));
